namespace Poser.UI;

/// <summary>
/// Font family selector. Cascades through nested elements.
/// Tabler is reserved for the follow-up pass that registers the Tabler webfont.
/// </summary>
public enum FontFamily
{
    Default,
    Italic,
    Mono,
    Icon,
}
