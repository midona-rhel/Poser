namespace Poser.Domain.Tests;

public sealed class SelectionScopeCharacterizationMarkerTests
{
    [Fact(Skip = "SelectionScope is an Application contract; its final tests belong to the Slice 2 Application test target.")]
    public void Selection_scope_characterization_marker()
    {
        Assert.True(false);
    }
}
