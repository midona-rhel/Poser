namespace Poser.UI;

/// <summary>Per-child override of the parent's <see cref="Align"/> AlignItems value.</summary>
public enum AlignSelf
{
    Auto,     // inherit from parent's AlignItems
    Start,
    Center,
    End,
    Stretch,
}
