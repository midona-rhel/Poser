# 0.9.8 release authorization and coverage

The user authorized the release on 2026-09-07 after requesting the final first-run
confirmation sentence change. Accepted integration base: `7195ea26`.
The latest deployed functional candidate was `a465187c`; the base adds only that
wording change. Earlier furniture, expression, camera and UI changes were accepted.

- Physics behavior was explicitly accepted; this release does not retune it.
- Live checks passed compact collider controls, independent collision/visibility,
  handle hiding, transform lock and duplicate properties.
- A library group saved and reloaded both colliders with matching relative spacing,
  opacity and different collision flags. Group entries appeared below spawn actions.
- Live bridge checks on a separate test actor verified that FABRIK and Rope target
  coordinates clamp under repeated excessive input and respond to a small reverse
  input without accumulated overshoot.
- The exact continuous held-mouse overshoot/reversal gesture was not completed live;
  the user stopped computer control before that check. Do not claim a live pass.
- Locked collider inspector fields display identity until unlocked. Locking itself
  prevents movement. The user accepted deferring this display issue.

Release compilation, tests, package audit, SBOM and publication checks run on the
exact release head. No Debug deployment is needed for release validation.
